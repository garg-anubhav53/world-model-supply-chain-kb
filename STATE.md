# STATE — checkpoint (read after kb/INDEX.md)
Updated: 2026-07-05 (Wave 3 complete; ROUTINE LIVE)
## ROUTINE LIVE: claude.ai cloud trigger trig_011qDWWCsA8PHMkFPgrLR1xm — daily 08:23 UTC, Opus 4.8, self-contained prompt. Cloud runs CANNOT read/write this repo → each run outputs ./report-latest.md; fold back into repo manually OR grant a repo token for auto-persistence.
Per-fire budget: ~6 Sonnet + ≤3 Opus, then checkpoint. **Time-box every agent; abandon if it runs long or the causal chain needs more caveats (log 'too-complicated').**

## STANDING PRIOR (never forget): >$50B forced capital is being injected → forced beneficiaries MUST exist. A dry run = SEARCH failure, not THESIS failure. Never conclude "no ideas."

## Sharpened hypothesis (evolves each run)
The un-crowded multiplier is a SMALL-base company with ONE concentrated forced design-win into a high-volume program — surfaced by BUYER-BOM TEARDOWN, not by scanning diversified component makers. (Wave-3 lesson: semi-obvious names were already-run or too-diversified.)

## Loop each fire
1. Read INDEX + STATE + dead-ends + lessons. Skip cached & fresh.
2. Top backlog task within budget. Sonnet=scout/price/gate; Opus=causal dive on a SMALL set; Haiku=extract/index.
3. **Elimination gate BEFORE Opus:** kill on financial distress, weak management, or GREATLY re-rated. Graded: not-moved=go, partial=higher bar, greatly-ran=drop.
4. Score survivors on the 4 GOAL gates (see ROUTINE §13). Persist to kb/ immediately.
5. Prune: files ~1 screen; raw to runs/<id>/. Curate REPORT (BUY/WATCH only, best first).
6. Commit & push. Checkpoint STATE. If a wave kills all → OPEN A NEW SOURCING VECTOR next, don't re-confirm kills.

## Task queue (priority)
1. **Diligence OMG.L (Oxford Metrics/Vicon)** — the best small-base multiplier fit from Wave 1, still UN-diligenced. Gate → causal → multiplication. (Directly refutes "no ideas.")
2. **Buyer-BOM teardown sourcing** (the sharpened method): pick a named buyer artifact (a Figure/Apptronik humanoid; a Waymo 6th-gen sensor pod) → decompose to named parts → find each part's supplier → gate → cache. Bias to small-base, single-design-win names.
3. 2nd-tier magnet recyclers MKA.TO / IXR.AX / UCU.V — gate then dive if clean (watch IP-licensing-speculation risk on process-tech names).
4. Re-verify HSAI / NOVT / ON tags if >7d stale.
5. Prune KB; curate REPORT.

## v3 loop is active (ROUTINE §14): SOURCE → cheap Haiku pre-gate → EV-rank → causal-rigor checklist → persist → pipeline (≥15 WATCH) → prune → commit. Never exit empty.
## Next-action pointer: pipeline <15 → FORCED SOURCING. Pop coverage-map vectors #1 (EDGAR significant-customer), #2 (Apptronik BOM), #5/#6 (Korea/Japan small-cap screens); run cheap pre-gate on all finds; diligence HSAI; EV-rank; grow pipeline toward 15. OMG.L done (WATCH). Kashifuji/Melexis/Neo/Mitsui/VPG killed.

## Wave 3 result: shortlist mostly KILLED (already-run: Neo/Mitsui; bump-not-multiply: Klingelnberg/Melexis; Kashifuji private). Thesis INTACT — pivot to smaller/earlier/concentrated names via buyer-BOM.
